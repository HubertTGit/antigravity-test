---
description: create authentication
---

- add passwordless authentication using clerk
- when user lands in home and not authenticated, user is required to authenticate
- if user is authenticated, user will be directed to dynamic route using userId
- this link is shareable 